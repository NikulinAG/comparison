mport com.sun.javafx.geom.AreaOp;
import com.sun.org.apache.xpath.internal.operations.Bool;

public class Loader
{
    public static void main(String[] args) {

        // ==================================================

        // то что есть в закромах родины

        Integer milkAmount = 200; // ml
        Integer powderAmount = 300; // g
        Integer eggsCount = 5; // items
        Integer sugarAmount = 5; // g
        Integer oilAmount = 30; // ml
        Integer appleCount = 8;

        // ====== Так похоже делать не стоило ? ? ============
        //Boolean milkAmount200 = true;
        //Boolean powderAmount5 = true;
        //Boolean eggsCount3 = true;
        //Boolean sugarAmount5 = true;
        //Boolean oilAmount30 = true;
        //Boolean appleCount8 = true;

        //================== БЛИНЫ =========================

        //powder - 400 g, sugar - 10 g, milk - 1 l, oil - 30 ml

        if ((powderAmount >= 400) && (sugarAmount >= 10) && (milkAmount >= 1) && (oilAmount >= 30)){
            System.out.println("Готовим блинчики");
        }
        else {
            System.out.println("Идем обедать в тратирЪ");
        }

        // ================== ОМЛЕТ =========================
        //milk - 300 ml, powder - 5 g, eggs - 5


        if ((milkAmount >= 300) && (powderAmount >= 5) && (eggsCount >= 5)){
            System.out.println("Готовим омлет");
        }
        else {
            System.out.println("Варим пельмени");
        }

        //================ Пирожок ==========================
        //apples - 3, milk - 100 ml, powder - 300 g, eggs - 4

        if ((appleCount >= 3) && (milkAmount >= 100) && (powderAmount >= 300) && (eggsCount >= 5)){
            System.out.println("Испечем пирог");
        }
        else {
            System.out.println("Сходим в пирожковую");
        }


    }
}
